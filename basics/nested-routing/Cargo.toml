[package]
name = "nested-routing"
version = "1.0.0"
edition = "2021"

[dependencies]
actix-web = { workspace = true, features = ["openssl"] }
env_logger.workspace = true
serde = { version = "^1.0", features = ["derive"] }
serde_json = "1.0.39"
