[package]
name = "template_yarte"
version = "0.0.1"
authors = ["Juan Aguilar Santillana <mhpoin@gmail.com>"]
publish = false
edition = "2018"

workspace = ".."

[dependencies]
env_logger = "0.7"

yarte = { version = "0.3", features=["with-actix-web"]  }

actix-web = "1.0"

[build-dependencies]
yarte = { version = "0.3", features=["with-actix-web"]  }

[dev-dependencies]
bytes = "0.4"
