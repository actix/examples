[package]
name = "actix-web-cors"
version = "1.0.0"
edition = "2021"

[dependencies]
actix-web = { workspace = true, features = ["rustls"] }
actix-cors = { workspace = true }

env_logger = "0.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
