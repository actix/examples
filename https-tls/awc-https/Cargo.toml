[package]
name = "awc_https"
version = "1.0.0"
edition = "2021"

[dependencies]
actix-web = { workspace = true }
awc = { version = "3", features = ["rustls"] }

env_logger.workspace = true
log.workspace = true
mime = "0.3"
rustls = "0.20"
webpki-roots = "0.22"
