[package]
name = "http-proxy"
version = "1.0.0"
edition = "2021"

[dependencies]
actix-web = { workspace = true, features = ["openssl"] }
awc = "3"

clap = { version = "4", features = ["derive"] }
env_logger.workspace = true
log.workspace = true
url = "2.2"
